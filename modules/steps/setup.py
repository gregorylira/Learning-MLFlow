from setuptools import setup, find_packages

setup(
    name='steps',
    version='0.1',
    packages=find_packages(),
    py_modules=['steps'],
    description='A module for data preprocessing and model training steps in a pipeline',
    author_email='seu@email.com',
    url='https://github.com/seu_usuario/Learning-MLFlow',
)
